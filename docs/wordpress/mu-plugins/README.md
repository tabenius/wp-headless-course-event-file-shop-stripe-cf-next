# RAGBAZ Bridge moved

The RAGBAZ Bridge plugin now lives in the monorepo under `packages/ragbaz-bridge-plugin/` (zip built to `packages/ragbaz-bridge-plugin/dist/`). The legacy copy was removed to avoid drift. Use the new path when copying into `wp-content/mu-plugins/` or `wp-content/plugins/`.
