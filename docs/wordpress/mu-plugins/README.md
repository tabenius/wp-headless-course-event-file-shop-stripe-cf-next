# RAGBAZ-Articulate moved

The RAGBAZ-Articulate plugin now lives in the monorepo under `packages/ragbaz-articulate-plugin/` (zip built to `packages/ragbaz-articulate-plugin/dist/`). The legacy copy was removed to avoid drift. Use the new path when copying into `wp-content/mu-plugins/` or `wp-content/plugins/`.
